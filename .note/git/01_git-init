https://git-scm.com/docs/git-init

git-init - Create an empty Git repository or reinitialize an existing one

git init -b master
Specify a initial branch name by "-b" option, the current default is "master"